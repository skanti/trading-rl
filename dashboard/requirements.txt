# Installs the repository package plus the dashboard-only dependencies. The
# google-api-core upper bound is declared by the dashboard extra because 2.35.0
# percent-encodes Firestore's special `(default)` database ID.
-e ..[dashboard]
